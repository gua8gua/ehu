---
description: Ehu 引擎 C++ 与引擎编码规范
globs: "**/*.cpp"
alwaysApply: false
---

# Ehu C++ 与引擎编码规范

## 命名与风格

- 类型：`PascalCase`（类、结构体、枚举）。
- 成员变量：`m_PascalCase`（如 `m_Window`、`m_LayerStack`）。
- 静态成员：`s_CamelCase` 或 `s_PascalCase`（如 `s_Instance`）。
- 命名空间：仅使用 `Ehu`，不要引入额外嵌套命名空间除非是子模块（如 `Ehu::Key`、`Ehu::Mouse` 已在 KeyCodes/MouseCodes 中使用）。

## 引擎 API 与头文件

- 需要被 SandBox 或其它外部目标调用的类/函数应使用 `EHU_API`（在 `Core.h` 中定义）。
- 引擎内部实现（如 `WindowsWindow`、`WindowsInput`）可以不用 `EHU_API`，仅通过抽象接口暴露。
- 新增事件类：必须包含 `EVENT_CLASS_TYPE(TypeName)` 和 `EVENT_CLASS_CATEGORY(...)`，并实现 `GetEventType()`、`GetName()`、`GetCategoryFlags()`（由宏提供即可）。

## 错误与日志

- 不要吞掉异常或错误：引擎内用 `EHU_CORE_ERROR` / `EHU_CORE_ASSERT`，客户端用 `EHU_ERROR` / `EHU_ASSERT`。
- 在关键路径（如窗口创建、GL 初始化）失败时使用 `EHU_CORE_ASSERT` 或明确日志后返回/抛出。

## 示例（事件处理）

```cpp
// 在 Layer 中处理事件
void OnEvent(Event& e) override {
    EventDispatcher d(e);
    if (d.Dispatch<MouseButtonPressedEvent>(EHU_BIND_EVENT_FN(MyLayer::OnMousePressed)))
        return;  // 已处理可提前返回，或依赖 e.Handled
}
```

## 示例（添加新 Layer）

```cpp
class GameLayer : public Ehu::Layer {
public:
    GameLayer() : Layer("GameLayer") {}
    void OnUpdate() override { /* 每帧逻辑 */ }
    void OnImGuiRender() override { /* 调试 UI */ }
    void OnEvent(Ehu::Event& e) override { /* 可选 */ }
};
// 在 Application 子类构造函数中：PushLayer(new GameLayer());
```

遵守上述规范可保持与现有 Ehu 代码风格一致，便于协作与维护。
