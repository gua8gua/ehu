
file(GLOB SRC_EHU_Platform_CPP "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
file(GLOB SRC_EHU_Platform_H   "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
set(SRC_EHU_Platform ${SRC_EHU_Platform_CPP} ${SRC_EHU_Platform_H})

target_sources(EhuLib PRIVATE ${SRC_EHU_Platform})
target_include_directories(EhuLib
    PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}
)